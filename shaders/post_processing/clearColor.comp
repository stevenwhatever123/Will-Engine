#version 450

layout (local_size_x = 16, local_size_y = 16) in;
layout (set = 0, binding = 0, rgba16) uniform image2D inputImage;

void main()
{
	ivec2 currentPixel = ivec2(gl_GlobalInvocationID.xy);

	vec4 res = vec4(0, 0, 0, 1);

	imageStore(inputImage, currentPixel, res);
}